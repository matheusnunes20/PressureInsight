/**
 * Two kinds of validation: structural validation of imported files
 * (columns, dates, numeric values) and business validation of a test
 * against its configured acceptance criteria (e.g. max 1% pressure drop
 * over 15 minutes).
 */
package com.pressuretestanalyzer.validation;
